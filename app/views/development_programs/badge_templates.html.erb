<section>
  <br>
  <h1 class="display-1 text-center fw-bold"> Acquired Badges </h1>
  <h4 class="text-uppercase mt-5 fw-bold"> DIGITAL TECHNOLOGIES </h2>
      <br>
    <div class="d-flex gap-4 flex-wrap">
        <% @digital_certs.each do |cert| %>

            <%= render "badges/badge_icon", width: 200, height: 200, level: cert.level, name: cert.training.localized_name, skill: cert.training.skill_id %>
            <br/>
        <% end %>
    </div>
    <br>
    <h4 class="text-uppercase mt-5 fw-bold"> MANUFACTURING TECHNOLOGIES </h2>
        <br>
    <div class="d-flex gap-4 flex-wrap">
        <% @manu_certs.each do |cert| %>

            <%= render "badges/badge_icon", width: 200, height: 200, level: cert.level, name: cert.training.localized_name, skill: cert.training.skill_id %>
            <br/>
        <% end %>
    </div>
    <br>
    <h4 class="text-uppercase mt-5 fw-bold"> PROFESSIONAL DEVELOPMENT </h2>
        <br>
    <div class="d-flex gap-4 flex-wrap">
        <% @prof_certs.each do |cert| %>

            <%= render "badges/badge_icon", width: 200, height: 200, level: cert.level, name: cert.training.localized_name, skill: cert.training.skill_id %>
            <br/>
        <% end %>
    </div>
    <br>
    <br>
    <h1 class="display-1 text-center fw-bold"> Remaining Badges </h1>

    <h4 class="text-uppercase mt-5 fw-bold"> DIGITAL TECHNOLOGIES </h2>
      <br>
    <div class="d-flex gap-4 flex-wrap">
        <% @digital_trainings.each do |cert| %>

            <%= render "badges/badge_icon", width: 200, height: 200, level: "none", name: cert.localized_name, skill: cert.skill_id %>
            <br/>
        <% end %>
    </div>
    <br>
    <h4 class="text-uppercase mt-5 fw-bold"> MANUFACTURING TECHNOLOGIES </h2>
        <br>
    <div class="d-flex gap-4 flex-wrap">
        <% @manu_trainings.each do |cert| %>

            <%= render "badges/badge_icon", width: 200, height: 200, level: "none", name: cert.localized_name, skill: cert.skill_id %>
            <br/>
        <% end %>
    </div>
    <br>
    <h4 class="text-uppercase mt-5 fw-bold"> PROFESSIONAL DEVELOPMENT </h2>
        <br>
    <div class="d-flex gap-4 flex-wrap">
        <% @prof_trainings.each do |cert| %>

            <%= render "badges/badge_icon", width: 200, height: 200, level: "none", name: cert.localized_name, skill: cert.skill_id %>
            <br/>
        <% end %>
    </div>
</section>